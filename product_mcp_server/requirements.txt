# Compatible with Python 3.10 and newer.
# Official MCP Python SDK (includes FastMCP and Streamable HTTP support).
mcp==1.28.1
# Explicit HTTP client used by ProductApiClient (also a transitive MCP dependency).
httpx==0.28.1
# Read-only stock tools reuse Backoffice SQLAlchemy models and engine helpers.
# Flask is pulled only because importing backoffice.app runs its package init;
# the MCP process never starts the Backoffice Flask application.
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
Werkzeug==3.1.3
Flask==3.1.0
# Test runner (aligned with the Backoffice test stack).
pytest==8.3.4
